name: Update BalticPulse energy news

on:
  workflow_dispatch:
  schedule:
    - cron: "7,37 * * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests

      - name: Fetch energy news
        run: python fetch_energy_news_snapshot.py

      - name: Commit news snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/energy_news.json
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Update BalticPulse energy news"
            git push
          fi
